val a: Int = 15;

fun fib(d: Int) : Int {
    if (d == 1 || d == 2) {
        return 1;
    }
    return fib (d - 1) + fib (d - 2);
}

fun main() : Unit {
  if (30 < 10) {
     println ("Jest");
  } else {
    println ("Nie jestem");
  }
  println( fib(3) );
}

fun foo() : Int { }
